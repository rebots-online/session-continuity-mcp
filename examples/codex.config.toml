# Add this block to ~/.codex/config.toml
# Replace <INSTALL_PATH> with the actual path to your clone

[mcp_servers.context-mcp]
enabled = true
command = "python3"
args = ["<INSTALL_PATH>/session-continuity-mcp/server.py"]
